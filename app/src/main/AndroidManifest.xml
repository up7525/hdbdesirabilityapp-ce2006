<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ntu.ce2006.swensens.hdbdesirabilityapp">

    <uses-permission android:name="android.permission.INTERNET" />
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".SearchActivity" />
        <activity android:name=".PinActivity" />
        <activity android:name=".HistoryActivity" />
        <activity android:name=".Search_Loc_Activity" />
        <activity android:name=".Search_Size_Activity" />
        <activity android:name=".Search_Price_Activity" />
        <activity android:name=".Search_Ameni_Activity" />
        <activity android:name=".ResultsActivity" />
        <activity android:name=".Info">



        </activity>
    </application>

</manifest>